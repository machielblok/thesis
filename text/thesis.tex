\documentclass[10pt,openright1]{tudelft}


%\usepackage{bophook}

%
%\usepackage{thumby}
%\thumbySides{two}
%\thumbyTotalChapters{10}
%\thumbyPageHeight{795}
%\thumbyThumbWidth{42.5}
%\thumbyForeground{white}
%\thumbyBackground{black}
%\thumbyNumberFormat{\Huge\textbf{$chapter number}}
%\thumbySetup



% for debugging
% \usepackage{showkeys} % Shows all labels
% \usepackage{layouts}
% our text width is 369.0 pts = 12.967 cm
\usepackage[table,xcdraw]{xcolor}
% for annotations in the WIP-version
\usepackage[]{todonotes}

% forcing figure placement
\usepackage{placeins}
% \FloatBarrier

% my preferred font, linux libertine
\usepackage{libertine}
\usepackage[T1]{fontenc}
%\usepackage[libertine]{newtxmath}

% more 
\usepackage{bbold}
\usepackage{enumitem}

% tables
\usepackage{tabu}
\usepackage{multirow}

%for using .eps
\usepackage{epstopdf}

%avoid word breaks
\usepackage{hyphenat}

% formatting the figure captions
\definecolor{ocre}{RGB}{77,99,104}
\usepackage[format=plain,font={sf,color=ocre},labelfont={bf,up}]{caption}
\DeclareCaptionLabelSeparator{bar}{ |}
\captionsetup{labelsep=bar}

% depth of the TOC
\setcounter{tocdepth}{4}
\usepackage[toc,page]{appendix}
\usepackage{etoolbox}
\appto\appendix{\addtocontents{toc}{\protect\setcounter{tocdepth}{0}}}

% reinstate the correct level for list of tables and figures
\appto\listoffigures{\addtocontents{lof}{\protect\setcounter{tocdepth}{1}}}
\appto\listoftables{\addtocontents{lot}{\protect\setcounter{tocdepth}{1}}}
% style of the section headings
%\usepackage{titlesec}
%\titleformat{\section}{\sffamily\bfseries\Large}{\thesection}{1em}{}

% where to look for figures (also, each chapter has their own)
\graphicspath{{figures/}} % Relative path for figures

% some unxiversal commands that are handy
\include{commands}

\begin{document}

% Front matter
\frontmatter
\include{frontmatter}

%% Main text
\mainmatter
%
%% this would work to remove numbers of the bibliography sections, but
%% also removes from the TOC
%% \renewcommand{\bibsection}{\section*{\bibname}}

\include{ch_introduction/introduction}

\include{ch_theory_and_methods/tam}

\include{ch_adptv_msmnt_cntrl/amc}
\include{ch_adptv_msmnt_magnetometry/amm}
\include{ch_LDE/LDE}
\include{ch_C13_dephasing_LDE/cdl}
\include{ch_carbon_dephasing_purified/cdp}

\include{ch_conclusion_and_outlook/conclusions}
\appendix

\include{ch_adptv_msmnt_cntrl_SI/amc_appendix}
\include{ch_adptv_msmnt_magnetometry_SI/amm_appendix}
\include{ch_LDE_SI/lde_appendix}
\include{ch_LDE_teleportation_SI/ch_LDE_teleportation_appendix}

\backmatter
\include{summary}
%\include{ch_at_the_end/acknowledgements}
\include{ch_at_the_end/publications}
\include{ch_at_the_end/cv}
%
\end{document}
